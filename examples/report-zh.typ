#import "../template.typ": *
#import "@preview/itemize:0.1.2" as el

#set text(lang: "zh")

#show: assignment-class.with(
  title: [*报告标题*],
  author: "作者姓名",
  course-name: "课程名称",
  mode: "report",
)

#show: el.default-enum-list
#show enum: block

#section[引言][
  本报告概述了与主题相关的关键概念和研究发现。

  本报告的目的是总结主要观点，并以清晰简明的方式呈现它们。
]

#section[方法论][
  方法论部分概述了用于收集和分析数据的方法与技术。

  它包含了研究阶段中所使用的工具和流程的描述。

  == 数据收集
  数据通过多种方式收集，包括问卷调查、访谈和观察。

  数据收集过程旨在确保准确性和可靠性。

  == 数据分析
  收集到的数据采用统计方法和定性分析技术进行分析。

  分析的目标是识别模式并从数据中得出有意义的结论。

  === 统计方法
  使用多种统计工具分析定量数据，包括回归分析和假设检验。

  === 定性分析
  对定性数据进行主题分析和内容分析，以提取重要主题和见解。

  === 使用的工具
  在数据分析阶段使用了以下工具：
  - 使用 Pandas 与 NumPy 等库的 Python
  - 用于统计计算的 R
  - 用于定性数据分析的 NVivo

  == 伦理考虑
  在整个研究过程中都考虑了伦理问题，以确保数据的完整性和保密性。

  参与者在数据收集前被告知研究目的并取得了他们的同意。

  == 局限性
  研究存在一定局限性，包括样本量有限和数据收集中的潜在偏差。

  这些局限性在研究结果的讨论中已被承认并进行了说明。
]

#section[结果][
  结果部分以结构化的形式呈现研究发现。

  它包含表格、图表和图形以说明收集到的数据。

  #figure(
    caption: [示例图。],
    rect(width: 240pt, height: 180pt)[
      #set align(center + horizon)
      XXX
    ]
  )

  分析得出若干关键见解，下面将详细讨论。
]
